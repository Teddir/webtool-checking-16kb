import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Android 16KB Page Size Checker",
  description: "Check if your Android APK or native libraries are compatible with 16KB page size requirements for Google Play compliance by November 1st, 2025.",
  keywords: ["Android", "16KB", "page size", "compatibility", "APK", "native libraries", "Google Play", "ELF alignment"],
  authors: [{ name: "Android 16KB Checker" }],
};

export const viewport = {
  width: 'device-width',
  initialScale: 1,
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
